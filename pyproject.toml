[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "regime-detection"
version = "0.1.0"
description = "MVP regime detection and prediction pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

